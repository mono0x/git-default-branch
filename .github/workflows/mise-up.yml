name: mise up
on:
  schedule:
    - cron: "0 0 * * 1" # every week
  workflow_dispatch:
jobs:
  mise-up:
    uses: mono0x/workflows/.github/workflows/mise-up.yml@78f096099d39a9687574ac7e655cf6c4e176d2f4 # main
    secrets:
      app-private-key: ${{ secrets.GH_APP_PRIVATE_KEY }}
    with:
      mise-version: "2025.11.8"
      app-id: ${{ vars.GH_APP_ID }}
