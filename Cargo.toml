[package]
name = "git-default-branch"
version = "0.20260608.58"
edition = "2024"

[profile.release]
lto = true
strip = "symbols"

[dependencies]
gix = { version = "0.78.0", default-features = false }
clap = { version = "4.5", features = ["derive"] }

[dev-dependencies]
tempfile = "3.24.0"
